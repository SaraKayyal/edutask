module.exports = {
  e2e: {
    setupNodeEvents(on, config) {
      // implement node event listeners here
      
    },
    viewportWidth: 1280, // Specify the width of the viewport here
    viewportHeight: 900, // Specify the height of the viewport here
  
  },
};
